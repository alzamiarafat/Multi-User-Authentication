@extends('layouts.app')

@section('content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header">
                    @if(Auth::user()->role == 'admin') {{ __('Admin Dashboard') }}
                    @elseif(Auth::user()->role == 'customer') {{ __('Customer Dashboard') }}
                    @elseif(Auth::user()->role == 'manager') {{ __('Manager Dashboard') }}
                    @endif
                </div>

                <div class="card-body">
                    @if (session('status'))
                        <div class="alert alert-success" role="alert">
                            {{ session('status') }}
                        </div>
                    @endif

                    {{ __('You are logged in!') }}
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
